{
  "name": "babel-plugin-transform-ternary",
  "version": "2.0.4",
  "description": "Transform ternary expressions into if-else statements.",
  "main": "lib/index.js",
  "scripts": {
    "lint": "eslint . --ext js,cjs",
    "lint:fix": "npm run lint -- --fix",
    "pretest": "npm run lint",
    "test": "npm run test:only -- --coverage",
    "test:only": "mocha --timeout 30000",
    "precommit": "npm run test",
    "prepublish": "npm run test",
    "postbuild": "npm run test",
    "build": "cd docs && npm install && npm run build && rimraf node_modules",
    "prepare": "npm run build"
  },
  "files": [
    "docs",
    "lib"
  ],
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/KarbitsCode/babel-plugin-transform-ternary.git"
  },
  "bugs": {
    "url": "https://github.com/KarbitsCode/babel-plugin-transform-ternary/issues"
  },
  "homepage": "https://github.com/KarbitsCode/babel-plugin-transform-ternary#readme",
  "keywords": [
    "babel-plugin"
  ],
  "author": "KarbitsCode",
  "license": "GPL-3.0",
  "dependencies": {
    "@babel/template": "^7.16.7"
  },
  "devDependencies": {
    "@babel/core": "^7.17.2",
    "chai": "^4.3.6",
    "eslint": "^8.8.0",
    "eslint-config-opengg": "^4.0.0",
    "eslint-plugin-import": "^2.25.4",
    "eslint-plugin-no-async-without-await": "^1.2.0",
    "husky": "^7.0.4",
    "mocha": "^9.2.0",
    "rimraf": "^6.0.1"
  }
}
